export enum OrderStatuses {
    PROCESSING = 'processing',
    ACCEPTED = 'accepted',
    ON_THE_WAY = 'on the way',
    DELIVERED = 'delivered',
    RECEIVED = 'received',
    RETURN = 'return',
    CANCELED = 'canceled'
}